---
export const prerender = false;
const model = Astro.url.searchParams.get('model');
const query = model ? `?model=${encodeURIComponent(model)}` : '';
return Astro.redirect(`/en/booking${query}`);
---
